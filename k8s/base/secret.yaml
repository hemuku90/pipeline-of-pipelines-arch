# NOTE: This is a template file only!
# In production, NEVER commit actual secrets to Git.
# Use External Secrets Operator, Sealed Secrets, or Vault.
# This file documents the required secrets structure.
---
apiVersion: v1
kind: Secret
metadata:
  name: pipeline-arch-secrets
  labels:
    app: pipeline-arch
    version: v1.0.0
  annotations:
    description: "Pipeline Architecture Application Secrets"
type: Opaque
stringData:
  # Database connection string
  # database-url: "postgresql://user:password@postgres:5432/pipeline_arch?sslmode=require"
  
  # Redis connection string
  # redis-url: "redis://redis:6379/0"
  
  # JWT secret for token signing
  # jwt-secret: "your-super-secret-jwt-key-change-in-production"
  
  # GitHub PAT for CI/CD workflows
  # github-token: "ghp_xxxxxxxxxxxx"
  
  # Docker registry credentials
  # docker-registry-username: "your-username"
  # docker-registry-password: "your-password-or-token"
  
  # Encryption key for sensitive data
  # encryption-key: "32-character-encryption-key-here"
  
  # API keys for external services
  # slack-webhook-url: "https://hooks.slack.com/services/xxx/yyy/zzz"
  # datadog-api-key: "your-datadog-api-key"
---
# Template for External Secrets (recommended for production)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: pipeline-arch-secrets
#   labels:
#     app: pipeline-arch
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: ClusterSecretStore
#   target:
#     name: pipeline-arch-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: database-url
#       remoteRef:
#         key: pipeline-arch/database-url
#         property: url
#     - secretKey: redis-url
#       remoteRef:
#         key: pipeline-arch/redis-url
#         property: url
#     - secretKey: jwt-secret
#       remoteRef:
#         key: pipeline-arch/jwt-secret
#         property: secret
---
# Example: Sealed Secrets (for git-safe secret storage)
# https://github.com/bitnami-labs/sealed-secrets
#
# 1. Install sealed secrets controller:
#    helm install sealed-secrets sealed-secrets/sealed-secrets -n kube-system
#
# 2. Create sealed secret:
#    kubeseal --format yaml < secret.yaml > sealed-secret.yaml
#
# 3. Commit sealed-secret.yaml to Git
# =============================================================================
# IMPORTANT SECURITY GUIDELINES
# =============================================================================
# 1. NEVER commit actual secrets to version control
# 2. Use Short-lived tokens when possible (GitHub PATs, etc.)
# 3. Rotate secrets regularly (quarterly at minimum)
# 4. Use different secrets for each environment
# 5. Enable audit logging for secret access
# 6. Use namespace isolation to limit secret exposure
# 7. Implement secret versioning for rotation
# =============================================================================